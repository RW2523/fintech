Write an Architecture Decision Record for: $ARGUMENTS

Create `docs/adr/NNNN-<slug>.md` (next number) using `docs/adr/0000-template.md`. Sections: Context, Decision, Alternatives considered, Consequences, Contract/policy changes (with schema version bumps), Tests affected. Reference the build task. Then update any affected docs under `docs/` so they stay the single source of truth, and note the ADR in `docs/PROGRESS.md`.
Do not proceed with the implementation until the ADR is written and committed (`adr: NNNN <slug>`).
