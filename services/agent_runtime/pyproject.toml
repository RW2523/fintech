[project]
name = "cio-svc-agent-runtime"
version = "0.1.0"
description = "Credit Intelligence OS — agent_runtime service"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
